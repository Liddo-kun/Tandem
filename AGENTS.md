# Tandem Agent Notes

All substantive Tandem repo guidance lives in `context.md`.

- At the start of a session, always read `context.md`.
- If editing package-specific code, also check the nearest nested `AGENTS.md` for that package.
- Keep this file as a small pointer only so it does not diverge from `context.md`.
